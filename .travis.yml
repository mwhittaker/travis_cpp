language: cpp
sudo: required
dist: trusty
install:
- ./scripts/install_dependencies.sh
script:
- rm -rf build && ./scripts/build Debug   1 && (cd build && make test)
- rm -rf build && ./scripts/build Release 1 && (cd build && make test)
